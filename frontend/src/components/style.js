import { makeStyles } from '@mui/styles';

function style() {
    const useStyles = makeStyles({
        login: {
            display: 'inline-grid',
            width: 100
        }
    })
}

export default style;